# Frontend Environment Configuration

# API URL Configuration
# For production: Use domain without port (nginx proxies /api/ internally)
# For development: Use :3000 port if API is exposed directly
# Examples:
#   Production: VITE_API_URL=https://maes-demo.ionsec.io
#   Development: VITE_API_URL=http://localhost:3000
VITE_API_URL=https://localhost